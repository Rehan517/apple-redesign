/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {nextConfig,
  reactStrictMode: true,
  images: {
    domains: ["rb.gy", "cdn.sanity.io"],
  },
}
